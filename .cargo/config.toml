[target.'cfg(all(target_arch = "arm", target_os = "none"))']
# replace STM32WBA65RI with your chip as listed in `probe-rs chip list`
runner = "probe-rs run --chip STM32WBA65RI"

[build]
target = "thumbv7em-none-eabihf"

[env]
DEFMT_LOG = "trace"
